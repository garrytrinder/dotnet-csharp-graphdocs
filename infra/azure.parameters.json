{
    "$schema": "https://schema.management.azure.com/schemas/2019-04-01/deploymentParameters.json#",
    "contentVersion": "1.0.0.0",
    "parameters": {
        "functionAppName": {
            "value": "func-graphdocs-prod-001"
        },
        "appServicePlanName": {
            "value": "asp-graphdocs-prod-001"
        },
        "storageAccountName": {
            "value": "stgraphdocs001"
        },
        "documentsApiUrl": {
            "value": "${{DOCUMENTS_API_URL}}"
        },
        "documentsApiScopes": {
            "value": "${{DOCUMENTS_API_SCOPES}}"
        },
        "entraTenantId": {
            "value": "${{ENTRA_APP_TENANT_ID}}"
        },
        "entraClientId": {
            "value": "${{ENTRA_APP_CLIENT_ID}}"
        },
        "entraClientSecret": {
            "value": "${{SECRET_ENTRA_APP_CLIENT_SECRET}}"
        }
    }
}